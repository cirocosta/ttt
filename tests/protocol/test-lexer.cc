#include "ttt/protocol/lexer.hh"
#include "gtest/gtest.h"

using namespace ttt;
using namespace ttt::protocol;

TEST(Lexer, Terminal)
{
  /* std::string msg = "\r\n"; */
  /* Buffer buf (msg); */

  ASSERT_EQ(1,1);
}

